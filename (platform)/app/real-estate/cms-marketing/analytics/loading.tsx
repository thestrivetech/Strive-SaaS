import { DashboardSkeleton } from '@/components/real-estate/content/shared/content-skeleton';

/**
 * Analytics Page Loading State
 *
 * Displayed while analytics data is being fetched
 */
export default function AnalyticsLoading() {
  return <DashboardSkeleton />;
}
