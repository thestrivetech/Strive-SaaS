{
  "name": "@strive/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types, schemas, and utilities for Strive Tech projects",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    "./types/*": "./types/*.ts",
    "./schemas/*": "./schemas/*.ts",
    "./services/*": "./services/*.ts"
  },
  "scripts": {
    "lint": "echo 'No lint configured for shared lib'",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf node_modules"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
